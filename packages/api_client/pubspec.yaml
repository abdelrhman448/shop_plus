name: api_client
description: >-
  A small, dependency-light HTTP client with interceptors (auth, logging,
  error transformation) and retry with exponential backoff.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  http: ^1.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
